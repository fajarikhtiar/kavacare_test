import 'package:flutter/material.dart';

class CareplanPage extends StatelessWidget {
  const CareplanPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Careplan Page'),
    );
  }
}
